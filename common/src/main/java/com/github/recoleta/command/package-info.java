/**
 * In-game commands exposing Recoleta state to operators.
 *
 * <p>Lets a server admin verify at runtime that the mod's mixins are
 * loaded, see how many strings are currently interned, inspect the
 * heap-pressure threshold and force an immediate cache/slack
 * compaction sweep.</p>
 */
package com.github.recoleta.command;

